@model ReseauSocial.Asp.Models.Forms.ProfileUser

@{
    ViewData["Title"] = "ProfilUser";
}

<h1>Votre profil</h1>

<div>
   
    <hr />
    <dl class="row">

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.LastName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.LastName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.FirstName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.FirstName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Email)
        </dd>
    </dl>
</div>
<div>
    <a asp-controller="User" asp-action="Update" asp-route-id="@Model.Id" class="btn btn-success">Modifier</a>


    @if (Model.Status.Id == (int)ReseauSocial.Asp.Models.StatusType.Actived)
    {
        <a asp-controller="User" asp-action="DeactivateStatus" asp-route-ChangedUserId="@Model.Id" class="btn btn-danger">Déactiver</a>
        <a asp-controller="User" asp-action="AskDeleteStatus"  asp-route-ChangedUserId="@Model.Id" class="btn btn-danger">Supprimer</a>

    }
    else if (Model.Status.Id == (int)ReseauSocial.Asp.Models.StatusType.Deactived)
    {
        <a asp-controller="User" asp-action="ReactivateStatus"  asp-route-ChangedUserId="@Model.Id" class="btn btn-primary">Réactiver</a>
    }
    else if (Model.Status.Id == (int)ReseauSocial.Asp.Models.StatusType.AskDeleted)
    {
        <a asp-controller="User" asp-action="ReactivateStatus"  asp-route-ChangedUserId="@Model.Id" class="btn btn-primary">Réactiver</a>       
    }
  
</div>
